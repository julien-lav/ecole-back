<% provide(:title, "À propos") %>
<%= render 'layouts/header' %>
<div class="ui container">
  <h1>Cours de première année</h1>
  <p>Find me in app/views/static_pages/about.html.erb</p>

  <p>4 blocks avec une image</p>
  <p>au click on ouvre une nouvelle pages</p>

</div>

<div class="ui container">
  <% if logged_in? %>
    Cours accessible une fois connecté !
  <% else %>
    Sommaire
  <% end %>
</div>

<%= render 'layouts/footer' %>
